# Aplikasi
PORT=8000

# Kunci signing JWT (WAJIB). Generate: openssl rand -base64 48
JWT_SECRET=

# Masa berlaku token dalam menit
JWT_EXPIRATION_MINUTES=60

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=lattepos
DB_SSLMODE=disable
